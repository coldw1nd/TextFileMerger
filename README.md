# TextFileMerger

Консольная утилита для рекурсивного слияния содержимого текстовых файлов в один файл

## Использование
```
TextFileMerger [output_path]
```

- Утилита сканирует текущую рабочую директорию
- Если `output_path` не указан, создается `output.txt` в текущей директории